version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - run:
          name: Build cmake
          command: |
            apk update |
            apk upgrade |
            apk add cmake
workflows:
  cpp-workflow:
    jobs:
      - build